# ==================== 生产环境 Secrets 示例 ====================
#
# 使用说明：
# 1. 复制此文件为 secrets.env
# 2. 填入实际的密钥值
# 3. 确保 secrets.env 已添加到 .gitignore（不要提交到 Git）
#
# 注意：这些密钥会被创建为 Kubernetes Secret
# ==================== 应用密钥 ====================

# JWT 密钥（必须）
# 生成方法: python -c 'import secrets; print(secrets.token_urlsafe(32))'
JWT_SECRET_KEY=<生成的强密钥>

# Fernet 加密密钥（必须）
# 生成方法: python -c 'from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())'
ENCRYPTION_KEY=<生成的 Fernet 密钥>

# ==================== 说明 ====================
#
# 数据库和 Redis 连接信息不需要在这里配置
# 它们会在运行时从 AWS Secrets Manager 自动获取
#
# RDS 配置: costq/rds/postgresql
# Redis 配置: costq/elasticache/redis
